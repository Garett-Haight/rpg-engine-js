export default class GameObject {
    constructor(args) {
        
    }

    buildGameObject (cfg) {
        var defaults = {
            type: 'entity',
            x: 0,
            y: 0
        };
        return defaults;
    };
    
    

    render(time, ctx) {
        
    }
}