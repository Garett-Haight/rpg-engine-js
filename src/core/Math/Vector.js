export default class Vector {
    constructor(x1, y1, x2, y2) {
        this._x = x || 0;
        this._y = y || 0;
    }

    getMagnitude() {
        return 1; //stub
        if (x1 === x2) { // straight lines are easier to calc

        }
        else if (y1 === y2) {

        }
        else {

        }
    }
}