class MapLayer {
	constructor(layer) {
		this.layer = layer;
		this.name = layer.name;
		this.width = layer.width;
		this.height = layer.height;
    }
    
    render() {
        
    }
}

export default MapLayer;