var buildGameObject = function(cfg) {
    var defaults = {
        type: 'entity',
        x: 0,
        y: 0
    };

    var name = cfg.name;
    var type = cfg.type || defaults.type;
    
} 

export default buildGameObject;